name: Issue 194 fix runner

on:
  push:
    branches:
      - feat/issue-194-vault-danger-zone
    paths:
      - .automation/issue-194-fix-run

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/issue-194-vault-danger-zone
      - name: Apply guarded fixes
        run: python .automation/issue-194-fix.py
      - name: Commit fixes
        run: |
          rm -f .automation/issue-194-fix-run .automation/issue-194-fix.py
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "fix: normalize vault danger stale errors"
          git push origin HEAD:feat/issue-194-vault-danger-zone
